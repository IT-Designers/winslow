import { LongLoadingDetector } from './long-loading-detector';

describe('LongLoadingDetector', () => {
  it('should create an instance', () => {
    expect(new LongLoadingDetector()).toBeTruthy();
  });
});
