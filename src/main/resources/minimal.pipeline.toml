[pipeline]
name = "minimal"
desc = "Testing a minimal set of functionality"

[pipeline.userInput]
valueFor = [
    "VIDEO_IN",
    "DATABASE_FILE"
]



[[stage]]
name = "print-env-variable"

[stage.image]
name = "ubuntu" # ENTRYPOINT python3 detector.py
args = ["/bin/bash", "-c", "echo 'hello from a docker image that is executed somehwere, managed by nomad and coordinated by winslow' && sleep 10 && date && echo bye"]

[stage.requires.gpu]
vendor = "nvidia"

