package de.itdesigners.winslow.asblr;

import static org.junit.Assert.assertThrows;

public class UserInputCheckerTest {
/*
    @Test
    public void testDetectionOfMissingConfirmation() {
        var checker = new UserInputChecker();
        var context = new Context(
                new Pipeline(),
                new PipelineDefinition(),
                null,
                new EnqueuedStage(),
                "some-stage-id",
                new DummyBuilder()
        );

        assertThrows(UserInputChecker.MissingUserConfirmationException.class, () -> checker.assemble(context));
    }

 */

}
